@{
    ViewData["Title"] = "Home";
    Layout = "_LandingLayout";
}

<section class="hero">
    <p class="hero__eyebrow">with Student Planner</p>
    <h1 class="hero__title">
        your all in one<br />
        <span>calendar</span>
    </h1>
    <p class="hero__subtitle">
        Student Planner combines all your tasks and to-dos
        into a single visual timeline.
    </p>
    <div class="hero__actions">
        <a class="hero__cta" asp-area="Identity" asp-page="/Account/Register">Start planning for free!</a>
    </div>
</section>
